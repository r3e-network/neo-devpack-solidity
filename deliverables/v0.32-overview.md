# v0.32 审计与重构阶段概览

## 已完成

- 生成 v0.32 增量 PRD 与实施架构文档。
- 修正 CHANGELOG 中审计脚本路径漂移。
- 建立生产 Rust 质量扫描：生产 `panic!/unwrap!/todo!/unimplemented!` 为硬门禁，`expect()` 采用 invariant/allowlist 审查。
- CI 接入生产文件长度、质量审计和 v0.31 回归测试门禁。
- 按自然职责拆分 IR 长文件：数组、返回 lowering、member calls、U256 softarith、builtin resolver。

## 静态验收结果

- 生产 Rust 超过 800 行文件：0。
- 测试超限文件：2 个，806 行和 926 行，当前非阻断并已报告。
- 质量审计：forbidden 0；生产 `expect()` 24 个；缺少 invariant/allowlist 0。
- 长度审计：显式长执行窗口下 exit 0。
- `git diff --check`：exit 0。
- 模块注册与关键抽取符号静态检查：通过。

## 未完成/后续

- 尚未运行 `cargo fmt --check`、`cargo check`、`cargo clippy`、`cargo test`，也未完成 C# 与 Neo-Express 回归；需要在允许写入 `target/` 的环境中执行。
- 尚未决定发布版本落点（0.31.0 或 0.32.0），未批量修改 Cargo 版本。
- `resolve_contract_type_name` 存在不同作用域同名 helper，非阻断维护观察项。
- 当前工作区仍包含原有未提交改动；本轮未提交 Git。
