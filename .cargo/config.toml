# Cargo configuration for Neo DevPack Solidity
# Optimized for development iteration speed with incremental compilation

[build]
# Enable incremental compilation for faster rebuilds
incremental = true

# Build profiles belong in Cargo.toml; Cargo ignores profile tables in this
# configuration file. Keeping them here previously gave a false impression
# that release LTO/debug settings were being applied.
